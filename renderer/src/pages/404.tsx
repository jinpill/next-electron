import Head from "next/head";

const NotFound = () => (
  <>
    <Head>
      <title>Not Found Window</title>
      <meta name="description" content="not found" />
    </Head>

    <main>
      <h1>Not Found</h1>
    </main>
  </>
);

export default NotFound;
